# 講師用進行メモ

対象: プログラミング初心者の大学生 約15人 / 60分 / Python・C++

---

## 事前準備（当日より前）

参加者へ事前に連絡しておくこと。

- Python（`python3 --version` が動く）か、C++（`g++ --version` が動く）のどちらかが使える状態にしておく
- 教材フォルダをダウンロードしておく
- **事前に `stack_sample.py` / `stack_sample.cpp` を1回実行しておく**

最後の1つが特に重要。
当日に環境構築が始まると15人は捌けない。

講師側の準備:

- `demo/bfs_visualizer.html` をブラウザで開いて表示確認（拡大表示も確認）
- `bfs_answer` / `bfs_path_answer` を Python・C++ 両方で実行しておく
- C++ は `-std=c++17` が必要（`auto [row, col] = ...` を使っているため）
- 会場で環境がない人向けに、オンライン実行環境のURLを1つ用意しておく

---

## タイムテーブル

| 時間 | 内容 | 位置づけ |
|---|---|---|
| 0〜4分 | 完成デモ | 必須 |
| 4〜7分 | 今日学ぶこと | 必須 |
| 7〜12分 | スタック・キュー | 必須 |
| 12〜19分 | ミニ演習 | 必須 |
| 19〜27分 | BFSをアニメーションで追う | 必須 |
| 27〜45分 | BFS穴埋め | 必須（本日の中心） |
| 45〜50分 | 最短距離の確認・到達不能の確認 | 必須 |
| 50〜55分 | 最短経路の復元（講師デモのみ） | 余裕があれば |
| 55〜58分 | 別の迷路で試す | 余裕があれば |
| 58〜60分 | DFS紹介・まとめ | 必須（口頭のみ） |

遅れた場合に削る順番:

1. 別の迷路で試す（55〜58分）
2. 最短経路の復元（50〜55分 → 「こういうこともできる」と画面で見せるだけ）
3. 迷路自動生成（もともと本編外。時間が余ったときだけ）

**穴埋め（27〜45分）は絶対に削らない。**

---

## 0〜4分: 完成デモ

`demo/bfs_visualizer.html` をブラウザで開く。

最初に伝えること:

- 今日作るものはこれ
- スタートからゴールまで何手か調べる
- 到達できるか調べる
- 最後に最短経路も出す

アニメーションでは必ず行番号・列番号を指しながら説明する。

「(row, col) = (行, 列)」を最初に固定する。

ここでは「自動再生（速い）」で全体の雰囲気だけ見せる。1手ずつは後で。

---

## 4〜7分: 今日学ぶこと

- スタック
- キュー
- BFS
- DFSは紹介だけ

---

## 7〜12分: スタック・キュー

スタック:
- 最後に入れたものから出る
- 本の積み重ねを例にする

キュー:
- 最初に入れたものから出る
- レジ待ちを例にする

BFSではキューを使う。

---

## 12〜19分: ミニ演習

`stack_queue_exercise.*`

ここは「環境が動くことの確認」も兼ねている。
開始2分で手が止まっている人がいないか、必ず歩いて確認する。

詰まっている人がいたら、

1. 追加する操作
2. 取り出す操作
3. 空になるまで繰り返す

の順にヒントを出す。

17分の時点で答えを画面に出し、全員を揃える。
ここで遅れを持ち越さない。

---

## 19〜27分: BFSを手で追う

アニメーションを1ステップずつ進める（← → キーでも進められる）。

説明ポイント:

1. queue にスタートを入れる
2. queue の先頭を取り出す（「今」のマス）
3. 上下左右を見る
4. 行ける場所を距離を付けて追加する（「待」のマス）
5. 一度見た場所（数字が入っているマス）は見ない

必ず queue の中身を指して説明する。

最初の10〜15ステップだけ手で進め、残りは自動再生に切り替える。
全67ステップを手で進めると時間が足りない。

「なぜ最短になるのか」は次の1文だけ伝える:

> キューは先に入れたものから出るので、近いマスから順に処理される。
> だから最初に着いたときの距離が最短になる。

---

## 27〜45分: BFS穴埋め（本日の中心）

`bfs_template.py` / `bfs_template.cpp`

TODO は Python・C++ とも 1〜7 で対応している。

| TODO | 内容 |
|---|---|
| 1 | キューから現在地を取り出す |
| 2 | 次の座標を求める |
| 3 | 迷路の外ならスキップ |
| 4 | 壁ならスキップ |
| 5 | 訪問済みならスキップ |
| 6 | 距離を記録する |
| 7 | キューに追加する |

正解の出力は `最短距離: 11`。

### ヒントは一気に全部出さない

- ヒント1: まず queue から現在地を取り出す（README「キューに座標を入れる」を見る）
- ヒント2: 上下左右の移動は `dr`, `dc` を使う
- ヒント3: 次の座標が迷路の中にあるか確認する（README にそのまま書いてある）
- ヒント4: `#` なら行けない
- ヒント5: `dist != -1` なら一度訪問済み
- ヒント6: 距離は `現在地 + 1`
- ヒント7: 最後に queue に追加

### 時間管理（15人いるので重要）

- **33分**: TODO 1〜3 の答えを画面に出す（全員を強制的に揃える）
- **40分**: TODO 4〜5 の答えを画面に出す
- **44分**: 全部の答えを出す。動かなかった人も `bfs_answer.*` で結果を確認させる

個別対応は1人あたり1分まで。
同じ質問が2人以上から出たら、その場で全体に説明する。

### よくあるつまずき

- Python: TODO を埋める前に実行して `IndentationError`（これは正常。埋めれば直る）
- C++: TODO を埋める前に実行すると**無限ループ**（`Ctrl + C` で止める）
- C++: `-std=c++17` を付け忘れてコンパイルエラー
- 訪問済みチェックを忘れる → 終わらない・距離が壊れる
- 距離を記録する前にキューへ追加 → 同じマスが何度も入る
- `dist[next_row][next_col]` と `dist[row][col]` の取り違え

---

## 45〜50分: 最短距離・到達不能

- `dist[goal_row][goal_col]` が最短距離であることを確認
- `maze` を `maze05_unreachable.txt` の内容に書き換えて実行すると `ゴールには到達できません`
- 「キューが空になれば必ず止まる。到達不能用の特別な処理は要らない」と伝える

---

## 50〜55分: 経路復元（余裕があれば）

`bfs_path_answer.*` を**講師が実行して見せるだけでよい**。
参加者に書かせない。

伝えること:

- `dist` と同じ形の表をもう2つ（`prev_row`, `prev_col`）用意する
- 「そのマスへどこから来たか」を記録する
- ゴールから逆にたどる

---

## 55〜58分: 別迷路で試す（余裕があれば）

`bfs_from_file.*` の `maze_file` を書き換えて実行する。

おすすめ:

- maze04: 少し複雑
- maze05_unreachable: 到達不能
- maze11_detour: 遠回りが必要（見た目は14手、実際は30手）

maze11_detour は可視化ツールにも入っている。
「まっすぐ行けそうに見えても最短は30手」は BFS の価値が一番伝わる例。

---

## 58〜60分: DFS紹介・まとめ

DFS:
- 深く進む
- stack / 再帰
- 到達できるかは分かるが、先に見つけた経路が最短とは限らない
- 最短距離目的なら今回はBFSが扱いやすい

最後に:
- BFS = キュー
- 近い場所から探索
- 訪問済み管理が必要

時間が余ったら `maze_generator.*` を動かして見せる（本編外）。

---

## 可視化ツールの使い方メモ

- 迷路は3種類切り替えられる（基本 / 到達できない / 遠回りが必要）
- 「ゴールに着いたら止める」のチェックを**外す**と、キューが空になるまで全マスに距離が広がる。
  参加者が書くコードはこちら（チェックなし）の動きと同じ。
- ← → キーで1手ずつ進む
- 凡例（今 / 待 / 数字 / ★）は画面下に常時表示

---

## 当日チェックリスト

開始前:

- [ ] プロジェクタで `bfs_visualizer.html` が読めるサイズか（ブラウザ拡大 125〜150%）
- [ ] ブラウザのダークモード拡張などで色が変わっていないか
- [ ] `python3 bfs_answer.py` が `最短距離: 11` を出す
- [ ] `g++ -std=c++17 -o bfs_answer bfs_answer.cpp && ./bfs_answer` が `最短距離: 11` を出す
- [ ] `bfs_path_answer` の出力（S / * / G の迷路）が両言語で一致している
- [ ] README の「実行方法」を参加者が開けているか
- [ ] Python組・C++組がそれぞれ何人か把握した

進行中:

- [ ] 19分: 全員がミニ演習を終えたか
- [ ] 33分: TODO 1〜3 を全体に開示したか
- [ ] 44分: 全員が `最短距離: 11` を見たか（自力で解けたかは問わない）
- [ ] 50分: 残り時間を見て、経路復元をやるか捨てるか決めたか
